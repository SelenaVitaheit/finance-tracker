<div class="container mt-4">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <div class="card">
        <div class="card-header <%= @category.category_type == 'income' ? 'bg-success' : 'bg-info' %> text-white">
          <h4 class="mb-0">
            <%= @category.category_type == 'income' ? '📈 Редактирование категории дохода' : '📉 Редактирование категории расхода' %>
          </h4>
        </div>
        <div class="card-body">
          <%= form_with(model: @category, local: true) do |form| %>
            <% if @category.errors.any? %>
              <div class="alert alert-danger">
                <h5><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h5>
                <ul>
                  <% @category.errors.each do |error| %>
                    <li><%= error.full_message %></li>
                  <% end %>
                </ul>
              </div>
            <% end %>
            
            <div class="mb-3">
              <%= form.label :name, "Название категории", class: "form-label" %>
              <%= form.text_field :name, class: "form-control" %>
            </div>
            
            <div class="mb-3">
              <label class="form-label">Тип категории</label>
              <div class="alert alert-info">
                <small>
                  <i class="bi bi-info-circle"></i>
                  Тип категории нельзя изменить. 
                  Текущий тип: <strong><%= @category.category_type == 'income' ? '📈 Доход' : '📉 Расход' %></strong>
                </small>
              </div>
            </div>
            
            <div class="d-grid gap-2 d-md-flex justify-content-md-end">
              <%= link_to '← Назад', categories_path, class: 'btn btn-outline-secondary me-2' %>
              <%= form.submit "Обновить категорию", class: "btn btn-primary" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>